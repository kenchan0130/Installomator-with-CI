name: CI
on:
  push:
    branches:
      - master
    tags:
      - "!*"
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run test
        run: |
          concurrency=$(sysctl -n hw.ncpu)
          cat Labels.txt | parallel --joblog joblog.txt --halt now,fail=1 --jobs ${concurrency} --tagstring {}: ./Installomator.sh {}
          cat joblog.txt
        env:
          GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
